// Paper material v1 (docs/04-GRAPHICS-PIPELINE.md §2.1): scanned page as
// baseColor + tiled grain normal map + high roughness. Show-through and
// per-GrainKind variants arrive in M7.
// Recompile: tools/filament/bin/matc -p mobile -a opengl -a vulkan \
//   -o filament/src/main/assets/materials/paper.filamat filament/src/main/materials/paper.mat
material {
    name : Paper,
    shadingModel : lit,
    requires : [ uv0 ],
    parameters : [
        { type : sampler2d, name : baseColorMap },
        { type : sampler2d, name : grainNormal },
        { type : float, name : roughness },
        { type : float, name : grainStrength },
        { type : float2, name : grainTiling }
    ]
}

fragment {
    void material(inout MaterialInputs material) {
        vec3 grain = texture(materialParams_grainNormal, getUV0() * materialParams.grainTiling).xyz * 2.0 - 1.0;
        material.normal = normalize(mix(vec3(0.0, 0.0, 1.0), grain, materialParams.grainStrength));
        prepareMaterial(material);
        material.baseColor = texture(materialParams_baseColorMap, getUV0());
        material.roughness = materialParams.roughness;
        material.metallic = 0.0;
    }
}
